[package]
name = "agent_account"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
soroban-sdk = { workspace = true }

[dev-dependencies]
ed25519-dalek = "2"
rand = "0.8"
soroban-sdk = { workspace = true, features = ["testutils"] }
